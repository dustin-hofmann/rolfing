---
import SharedNavBar from './SharedNavBar.astro';
import MainNavBar from './MainNavBar.astro';

interface Props {
  isHomePage?: boolean;
}

const { isHomePage = false } = Astro.props;
---

<SharedNavBar />
<MainNavBar isHomePage={isHomePage} />