---
---

<a
  href="https://wa.me/4915563331898?text=Hallo,%20ich%20interessiere%20mich%20für%20eine%20Rolfing-Behandlung%20und%20möchte%20gerne%20einen%20Termin%20vereinbaren."
  target="_blank"
  rel="noopener noreferrer"
  class="whatsapp-button"
  aria-label="Kontakt via WhatsApp"
>
  <i class="bi bi-whatsapp"></i>
</a>

<style>
  .whatsapp-button {
    position: fixed;
    bottom: 90px;
    right: 20px;
    width: 60px;
    height: 60px;
    background: linear-gradient(135deg, #25D366 0%, #20BA5A 100%);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-size: 32px;
    box-shadow: 0 4px 20px rgba(37, 211, 102, 0.4);
    z-index: 999;
    transition: all 0.3s ease;
    text-decoration: none;
    animation: slideInRight 0.5s ease-out;
  }

  .whatsapp-button:hover {
    transform: scale(1.1);
    box-shadow: 0 6px 30px rgba(37, 211, 102, 0.6);
    color: white;
  }

  .whatsapp-button i {
    line-height: 1;
  }

  @keyframes slideInRight {
    from {
      transform: translateX(100px);
      opacity: 0;
    }
    to {
      transform: translateX(0);
      opacity: 1;
    }
  }

  @keyframes pulse-whatsapp {
    0%, 100% {
      box-shadow: 0 4px 20px rgba(37, 211, 102, 0.4);
    }
    50% {
      box-shadow: 0 6px 30px rgba(37, 211, 102, 0.7);
    }
  }

  .whatsapp-button {
    animation: slideInRight 0.5s ease-out, pulse-whatsapp 2s infinite 1s;
  }

  @media (max-width: 768px) {
    .whatsapp-button {
      bottom: 80px;
      right: 15px;
      width: 55px;
      height: 55px;
      font-size: 28px;
    }
  }
</style>

